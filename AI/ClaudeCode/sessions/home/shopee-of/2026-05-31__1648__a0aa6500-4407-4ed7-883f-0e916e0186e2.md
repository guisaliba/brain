---
session_id: a0aa6500-4407-4ed7-883f-0e916e0186e2
project: shopee-of
machine: home
date: 2026-05-31
started: 2026-05-31 16:48:53 UTC
git_repo: shopee-of
git_branch: main
git_dirty: True
git_ahead: 0
git_has_remote: True
---

# Session: shopee-of / a0aa6500

## Files Touched

- `~/.claude/skills/setup-matt-pocock-skills/issue-tracker-github.md` (read, doc)
- `~/.claude/skills/setup-matt-pocock-skills/triage-labels.md` (read, doc)
- `~/.claude/skills/setup-matt-pocock-skills/domain.md` (read, doc)
- `~/projects/active/aurea/shopee-of/CLAUDE.md` (write, doc)

## Transcript

### User _2026-05-31 16:48:53 UTC_

<command-message>setup-matt-pocock-skills</command-message>
<command-name>/setup-matt-pocock-skills</command-name>

### User _2026-05-31 16:48:53 UTC_

Base directory for this skill: ~/.claude/skills/setup-matt-pocock-skills

# Setup Matt Pocock's Skills

Scaffold the per-repo configuration that the engineering skills assume:

- **Issue tracker** — where issues live (GitHub by default; local markdown is also supported out of the box)
- **Triage labels** — the strings used for the five canonical triage roles
- **Domain docs** — where `CONTEXT.md` and ADRs live, and the consumer rules for reading them

This is a prompt-driven skill, not a deterministic script. Explore, present what you found, confirm with the user, then write.

## Process

### 1. Explore

Look at the current repo to understand its starting state. Read whatever exists; don't assume:

- `git remote -v` and `.git/config` — is this a GitHub repo? Which one?
- `AGENTS.md` and `CLAUDE.md` at the repo root — does either exist? Is there already an `## Agent skills` section in either?
- `CONTEXT.md` and `CONTEXT-MAP.md` at the repo root
- `docs/adr/` and any `src/*/docs/adr/` directories
- `docs/agents/` — does this skill's prior output already exist?
- `.scratch/` — sign that a local-markdown issue tracker convention is already in use

### 2. Present findings and ask

Summarise what's present and what's missing. Then walk the user through the three decisions **one at a time** — present a section, get the user's answer, then move to the next. Don't dump all three at once.

Assume the user does not know what these terms mean. Each section starts with a short explainer (what it is, why these skills need it, what changes if they pick differently). Then show the choices and the default.

**Section A — Issue tracker.**

> Explainer: The "issue tracker" is where issues live for this repo. Skills like `to-issues`, `triage`, `to-prd`, and `qa` read from and write to it — they need to know whether to call `gh issue create`, write a markdown file under `.scratch/`, or follow some other workflow you describe. Pick the place you actually track work for this repo.

Default posture: these skills were designed for GitHub. If a `git remote` points at GitHub, propose that. If a `git remote` points at GitLab (`gitlab.com` or a self-hosted host), propose GitLab. Otherwise (or if the user prefers), offer:

- **GitHub** — issues live in the repo's GitHub Issues (uses the `gh` CLI)
- **GitLab** — issues live in the repo's GitLab Issues (uses the [`glab`](https://gitlab.com/gitlab-org/cli) CLI)
- **Local markdown** — issues live as files under `.scratch/<feature>/` in this repo (good for solo projects or repos without a remote)
- **Other** (Jira, Linear, etc.) — ask the user to describe the workflow in one paragraph; the skill will record it as freeform prose

**Section B — Triage label vocabulary.**

> Explainer: When the `triage` skill processes an incoming issue, it moves it through a state machine — needs evaluation, waiting on reporter, ready for an AFK agent to pick up, ready for a human, or won't fix. To do that, it needs to apply labels (or the equivalent in your issue tracker) that match strings *you've actually configured*. If your repo already uses different label names (e.g. `bug:triage` instead of `needs-triage`), map them here so the skill applies the right ones instead of creating duplicates.

The five canonical roles:

- `needs-triage` — maintainer needs to evaluate
- `needs-info` — waiting on reporter
- `ready-for-agent` — fully specified, AFK-ready (an agent can pick it up with no human context)
- `ready-for-human` — needs human implementation
- `wontfix` — will not be actioned

Default: each role's string equals its name. Ask the user if they want to override any. If their issue tracker has no existing labels, the defaults are fine.

**Section C — Domain docs.**

> Explainer: Some skills (`improve-codebase-architecture`, `diagnose`, `tdd`) read a `CONTEXT.md` file to learn the project's domain language, and `docs/adr/` for past architectural decisions. They need to know whether the repo has one global context or multiple (e.g. a monorepo with separate frontend/backend contexts) so they look in the right place.

Confirm the layout:

- **Single-context** — one `CONTEXT.md` + `docs/adr/` at the repo root. Most repos are this.
- **Multi-context** — `CONTEXT-MAP.md` at the root pointing to per-context `CONTEXT.md` files (typically a monorepo).

### 3. Confirm and edit

Show the user a draft of:

- The `## Agent skills` block to add to whichever of `CLAUDE.md` / `AGENTS.md` is being edited (see step 4 for selection rules)
- The contents of `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`, `docs/agents/domain.md`

Let them edit before writing.

### 4. Write

**Pick the file to edit:**

- If `CLAUDE.md` exists, edit it.
- Else if `AGENTS.md` exists, edit it.
- If neither exists, ask the user which one to create — don't pick for them.

Never create `AGENTS.md` when `CLAUDE.md` already exists (or vice versa) — always edit the one that's already there.

If an `## Agent skills` block already exists in the chosen file, update its contents in-place rather than appending a duplicate. Don't overwrite user edits to the surrounding sections.

The block:

```markdown
## Agent skills

### Issue tracker

[one-line summary of where issues are tracked]. See `docs/agents/issue-tracker.md`.

### Triage labels

[one-line summary of the label vocabulary]. See `docs/agents/triage-labels.md`.

### Domain docs

[one-line summary of layout — "single-context" or "multi-context"]. See `docs/agents/domain.md`.
```

Then write the three docs files using the seed templates in this skill folder as a starting point:

- [issue-tracker-github.md](./issue-tracker-github.md) — GitHub issue tracker
- [issue-tracker-gitlab.md](./issue-tracker-gitlab.md) — GitLab issue tracker
- [issue-tracker-local.md](./issue-tracker-local.md) — local-markdown issue tracker
- [triage-labels.md](./triage-labels.md) — label mapping
- [domain.md](./domain.md) — domain doc consumer rules + layout

For "other" issue trackers, write `docs/agents/issue-tracker.md` from scratch using the user's description.

### 5. Done

Tell the user the setup is complete and which engineering skills will now read from these files. Mention they can edit `docs/agents/*.md` directly later — re-running this skill is only necessary if they want to switch issue trackers or restart from scratch.


### Assistant _2026-05-31 16:48:55 UTC_

I'll explore the repo first to understand what's already in place.

### Assistant _2026-05-31 16:48:57 UTC_

> **Bash**: Check git remote and existing doc files

### User _2026-05-31 16:48:57 UTC_

### Assistant _2026-05-31 16:48:58 UTC_

> **Bash**: Check docs and scratch directories

### User _2026-05-31 16:48:58 UTC_

### Assistant _2026-05-31 16:49:00 UTC_

> **Bash**: List markdown files at root

### User _2026-05-31 16:49:00 UTC_

### Assistant _2026-05-31 16:49:04 UTC_

Now let me read the seed templates.

### Assistant _2026-05-31 16:49:05 UTC_

> **Bash**: List seed templates in skill folder

### User _2026-05-31 16:50:09 UTC_

### Assistant _2026-05-31 16:50:11 UTC_

> **Read**: `~/.claude/skills/setup-matt-pocock-skills/issue-tracker-github.md`

### Assistant _2026-05-31 16:50:12 UTC_

> **Read**: `~/.claude/skills/setup-matt-pocock-skills/triage-labels.md`

### Assistant _2026-05-31 16:50:12 UTC_

> **Read**: `~/.claude/skills/setup-matt-pocock-skills/domain.md`

### User _2026-05-31 16:50:14 UTC_

### User _2026-05-31 16:50:14 UTC_

### User _2026-05-31 16:50:14 UTC_

### Assistant _2026-05-31 16:50:23 UTC_

Here's what I found in this repo:

- **Remote**: `git@github.com:Aurea-Ecom-Labs/shopee-of.git` (GitHub)
- **CLAUDE.md / AGENTS.md**: neither exists yet
- **CONTEXT.md / CONTEXT-MAP.md**: neither exists yet
- **docs/adr/**, **docs/agents/**, **.scratch/**: none exist

Everything needs to be created from scratch. Let me walk you through the three decisions one at a time.

---

## Section A — Issue tracker

The "issue tracker" is where issues (bugs, features, PRDs) live for this repo. Skills like `to-issues`, `triage`, `to-prd`, and `qa` need to know whether to call `gh issue create`, write markdown files locally, or follow some other workflow. Pick the place you actually track work.

Since your remote points at GitHub (`Aurea-Ecom-Labs/shopee-of`), the default is **GitHub Issues** using the `gh` CLI.

### Assistant _2026-05-31 16:50:27 UTC_

> **AskUserQuestion**: questions=[{'question': 'Where should issues be tr

### User _2026-05-31 16:50:49 UTC_

### Assistant _2026-05-31 16:50:54 UTC_

**GitHub** it is.

---

## Section B — Triage label vocabulary

When the `triage` skill processes an issue, it applies labels to move it through a state machine. It needs label strings that match what's actually configured in your GitHub repo. The five canonical roles are:

| Role | Default label string | Meaning |
|---|---|---|
| Needs evaluation | `needs-triage` | Maintainer reviews it |
| Waiting on reporter | `needs-info` | Need more info |
| AFK-agent ready | `ready-for-agent` | Fully spec'd, agent can pick it up |
| Human-ready | `ready-for-human` | Needs human implementation |
| Won't fix | `wontfix` | Will not be actioned |

### Assistant _2026-05-31 16:50:56 UTC_

> **AskUserQuestion**: questions=[{'question': "Do you want to use the de

### User _2026-05-31 16:51:12 UTC_

### Assistant _2026-05-31 16:51:20 UTC_

**Default labels** confirmed.

---

## Section C — Domain docs

Some skills (`improve-codebase-architecture`, `diagnose`, `tdd`, `grill-with-docs`) read a `CONTEXT.md` file to learn the project's domain language and `docs/adr/` for past architectural decisions. They need to know whether there's one global context or multiple (e.g. a monorepo with separate frontend/backend contexts).

- **Single-context** — one `CONTEXT.md` + `docs/adr/` at the repo root. Most repos are this.
- **Multi-context** — a `CONTEXT-MAP.md` at the root pointing to per-context files (typically a monorepo).

Neither `CONTEXT.md` nor `CONTEXT-MAP.md` exists yet — that's fine, skills will create them lazily as you use `/grill-with-docs`.

### Assistant _2026-05-31 16:51:21 UTC_

> **AskUserQuestion**: questions=[{'question': "What's the domain doc lay

### User _2026-05-31 16:51:35 UTC_

### Assistant _2026-05-31 16:51:39 UTC_

**Single-context** confirmed.

---

Since neither `CLAUDE.md` nor `AGENTS.md` exists yet, I need to ask which one to create before showing the draft.

### Assistant _2026-05-31 16:51:42 UTC_

> **AskUserQuestion**: questions=[{'question': 'Which file should be crea

### User _2026-05-31 16:51:51 UTC_

### Assistant _2026-05-31 16:51:57 UTC_

Here's the draft of everything that will be written. Review before I proceed:

---

**`CLAUDE.md`** (new file, `## Agent skills` section):

```markdown
## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues for this repo (`Aurea-Ecom-Labs/shopee-of`). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage labels using default strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` + `docs/adr/` at the root. See `docs/agents/domain.md`.
```

---

**`docs/agents/issue-tracker.md`** — GitHub template (as-is from seed)

**`docs/agents/triage-labels.md`** — Default label mapping (as-is from seed)

**`docs/agents/domain.md`** — Single-context consumer rules (as-is from seed)

---

Everything look good? I'll write all four files now.

### Assistant _2026-05-31 16:51:58 UTC_

> **AskUserQuestion**: questions=[{'question': 'Ready to write these file

### User _2026-05-31 16:52:04 UTC_

### Assistant _2026-05-31 16:52:07 UTC_

> **Bash**: Create docs/agents directory

### User _2026-05-31 16:52:07 UTC_

### Assistant _2026-05-31 16:52:10 UTC_

> **Write**: `~/projects/active/aurea/shopee-of/CLAUDE.md`

### User _2026-05-31 16:52:10 UTC_

### Assistant _2026-05-31 16:52:13 UTC_

> **Bash**: Copy seed templates to docs/agents/

### User _2026-05-31 16:52:15 UTC_

### Assistant _2026-05-31 16:52:19 UTC_

All four files written:

- `CLAUDE.md` — created with `## Agent skills` block
- `docs/agents/issue-tracker.md` — GitHub conventions (`gh` CLI)
- `docs/agents/triage-labels.md` — five default label strings
- `docs/agents/domain.md` — single-context consumer rules

The following skills will now read from these files: **`to-issues`**, **`triage`**, **`to-prd`**, **`qa`**, **`improve-codebase-architecture`**, **`diagnose`**, **`tdd`**, and **`grill-with-docs`**. You can edit any file under `docs/agents/` directly if your workflow changes — re-running this skill is only needed if you want to switch issue trackers or start over.

### User _2026-05-31 17:05:06 UTC_

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### User _2026-05-31 17:05:06 UTC_

<command-name>/model</command-name>
            <command-message>model</command-message>
            <command-args></command-args>

### User _2026-05-31 17:05:06 UTC_

<local-command-stdout>Set model to [1mOpus 4.8 (1M context)[22m and saved as your default for new sessions</local-command-stdout>
